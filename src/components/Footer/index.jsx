import React from "react";
import "./index.css"

const Footer = () => {
    // const { user } = useContext(UserCtx);

    return (
        <>
        <footer>
            Подвал
        </footer>

        </>
    )
}

export default Footer;